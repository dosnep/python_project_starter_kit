import setuptools

setuptools.setup(
        name="project",
    version="0.0.1",
    keywords=["project"],
    install_requires=[
        "Flask==1.1.2"
    ],    
    packages=setuptools.find_packages(),
)

